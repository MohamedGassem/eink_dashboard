# eink-dashboard

A FastAPI backend for serving TCL transit and Vélo'v bike-share data to an e-ink display.

Create the local environment file, fill in its values, then start Docker:

- Windows PowerShell: `Copy-Item .env.example .env`
- WSL/Linux: `cp .env.example .env`
- All environments: `docker compose up -d`
