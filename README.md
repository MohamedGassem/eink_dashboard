# eink-dashboard

A FastAPI backend for serving TCL transit and Vélo'v bike-share data to an e-ink display.

To run the application: `cp .env.example .env` (fill in the values), then `docker compose up -d`
