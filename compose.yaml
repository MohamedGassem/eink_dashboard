services:
  dashboard:
    build: .
    image: eink-dashboard:latest
    restart: unless-stopped
    ports:
      - "9001:9001"
    env_file:
      - .env
    volumes:
      - ./config:/app/config:ro
